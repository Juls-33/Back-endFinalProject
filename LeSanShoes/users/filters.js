// $('#colorwaySearch').on('input', function () {
//         const searchTerm = $(this).val().toLowerCase();
//         $('.colorway-card').each(function () {
//             const text = $(this).attr('data-search').toLowerCase();
//             $(this).toggle(text.includes(searchTerm));
//         });
//     });

// $('#Anta').on('change', function () {
//     const isChecked = $(this).is(':checked');
//     const searchTerm = $(this).val().toLowerCase();
//     $('.colorway-card').each(function () {
//         const text = $(this).attr('data-search').toLowerCase();

//         if (isChecked) {
//             $(this).toggle(text.includes(searchTerm));
//         } else {
//             $(this).show(); // show everything when unchecked
//         }
//     });
// });

// $('#Nike').on('change', function () {
//     const isChecked = $(this).is(':checked');
//     const searchTerm = $(this).val().toLowerCase();
//     $('.colorway-card').each(function () {
//         const text = $(this).attr('data-search').toLowerCase();

//         if (isChecked) {
//             $(this).toggle(text.includes(searchTerm));
//         } else {
//             $(this).show(); // show everything when unchecked
//         }
//     });
// });
// $('#Under').on('change', function () {
//     const isChecked = $(this).is(':checked');
//     const searchTerm = $(this).val().toLowerCase();
//     $('.colorway-card').each(function () {
//         const text = $(this).attr('data-search').toLowerCase();

//         if (isChecked) {
//             $(this).toggle(text.includes(searchTerm));
//         } else {
//             $(this).show(); // show everything when unchecked
//         }
//     });
// });
// $('#Adidas').on('change', function () {
//     const isChecked = $(this).is(':checked');
//     const searchTerm = $(this).val().toLowerCase();
//     $('.colorway-card').each(function () {
//         const text = $(this).attr('data-search').toLowerCase();

//         if (isChecked) {
//             $(this).toggle(text.includes(searchTerm));
//         } else {
//             $(this).show(); // show everything when unchecked
//         }
//     });
// });
// $('#Asics').on('change', function () {
//     const isChecked = $(this).is(':checked');
//     const searchTerm = $(this).val().toLowerCase();
//     $('.colorway-card').each(function () {
//         const text = $(this).attr('data-search').toLowerCase();

//         if (isChecked) {
//             $(this).toggle(text.includes(searchTerm));
//         } else {
//             $(this).show(); // show everything when unchecked
//         }
//     });
// });